# IMPORTANT: Replace placeholder values with real keys before deploying.
# For production, use an external secret manager (e.g. Sealed Secrets, External Secrets Operator).
apiVersion: v1
kind: Secret
metadata:
  name: deer-flow-secrets
  namespace: deer-flow
type: Opaque
stringData:
  TAVILY_API_KEY: ""
  JINA_API_KEY: ""
  INFOQUEST_API_KEY: ""
  GEMINI_API_KEY: ""        # Your Google AI API key
  DEEPSEEK_API_KEY: ""      # Your DeepSeek API key
  BETTER_AUTH_SECRET: ""    # Generate with: openssl rand -hex 32
